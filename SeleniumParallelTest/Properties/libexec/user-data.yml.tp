#cloud-config
hostname: ${hostname}
fqdn: ${fqdn}

runcmd:
  - |
    echo 'interface "eth0" {
      supersede interface-mtu 1372;
      supersede domain-search "${dns_search}";
    }' >> /etc/dhcp/dhclient-eth0.conf
